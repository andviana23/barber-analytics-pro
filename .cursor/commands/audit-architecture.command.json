{
  "name": "audit-architecture",
  "description": "Audita todo o sistema e aponta violações de arquitetura (acoplamento, duplicidade, violações de SOLID, etc.)",
  "prompt": "# 🔍 AUDITORIA DE ARQUITETURA\n\nVocê deve realizar uma **auditoria completa** do sistema **BARBER-ANALYTICS-PRO** e identificar violações dos princípios de **Clean Architecture**, **SOLID**, **DDD** e **CQRS**.\n\n---\n\n## 🎯 Objetivo\n\nIdentificar e reportar:\n1. ❌ **Violações de Clean Architecture**\n2. ❌ **Violações de SOLID**\n3. ❌ **Acoplamento desnecessário**\n4. ❌ **Código duplicado**\n5. ❌ **Inconsistências de nomenclatura**\n6. ❌ **DTOs mal implementados**\n7. ❌ **Services acessando banco diretamente**\n8. ❌ **Componentes React chamando Repository**\n9. ❌ **Falta de validação**\n10. ❌ **Segurança (RLS, SQL Injection, XSS)**\n\n---\n\n## 📋 Checklist de Auditoria\n\n### **1. Clean Architecture**\n\n#### **1.1. Dependências Invertidas**\n```javascript\n// ❌ ERRADO: Service depende de implementação concreta\nimport { supabase } from './supabase';\nclass FinanceiroService {\n  async create(data) {\n    return await supabase.from('revenues').insert(data);\n  }\n}\n\n// ✅ CORRETO: Service depende de abstração (Repository)\nimport revenueRepository from '../repositories/revenueRepository';\nclass FinanceiroService {\n  async create(data) {\n    return await revenueRepository.create(data);\n  }\n}\n```\n\n**Comando de busca:**\n```bash\n# Procurar Services que importam 'supabase' diretamente\ngrep -r \"import.*supabase\" src/services/ --exclude=\"supabase.js\"\n```\n\n#### **1.2. Camadas Bem Definidas**\n\nVerificar se as camadas estão isoladas:\n\n```\n✅ CORRETO:\nPage → Hook → Service → Repository → Supabase\n\n❌ ERRADO:\nPage → Service → Supabase (pulou Repository)\nPage → Repository (pulou Service E Hook)\nHook → Repository (pulou Service)\n```\n\n**Comando de busca:**\n```bash\n# Procurar hooks que importam repositories\ngrep -r \"import.*Repository\" src/hooks/\n\n# Procurar pages que importam services diretamente\ngrep -r \"import.*Service\" src/pages/\n```\n\n---\n\n### **2. SOLID**\n\n#### **2.1. Single Responsibility Principle (SRP)**\n\n```javascript\n// ❌ ERRADO: Service fazendo validação + persistência + envio de email\nclass FinanceiroService {\n  async createReceita(data) {\n    // Validação\n    if (!data.value) throw new Error('Invalid');\n    \n    // Persistência\n    await supabase.from('revenues').insert(data);\n    \n    // Email (responsabilidade extra!)\n    await sendEmail(data.user_email, 'Receita criada');\n  }\n}\n\n// ✅ CORRETO: Service apenas orquestra, delegando responsabilidades\nclass FinanceiroService {\n  async createReceita(data) {\n    const dto = new CreateRevenueDTO(data);\n    dto.validate(); // DTO valida\n    const result = await revenueRepository.create(dto.toDatabase()); // Repository persiste\n    await emailService.sendConfirmation(result.data); // EmailService envia\n    return result;\n  }\n}\n```\n\n**Comando de busca:**\n```bash\n# Procurar services com mais de 300 linhas (possível violação SRP)\nfind src/services -name \"*.js\" -exec wc -l {} \\; | awk '$1 > 300 {print $2, \"(\", $1, \"linhas)\"}'\n```\n\n#### **2.2. Open/Closed Principle (OCP)**\n\n```javascript\n// ❌ ERRADO: Precisa modificar função para adicionar novo tipo\nfunction calculateFee(type) {\n  if (type === 'credit_card') return 3.5;\n  if (type === 'debit_card') return 2.0;\n  // Precisa modificar para adicionar 'pix'\n}\n\n// ✅ CORRETO: Extensível sem modificar código existente\nconst FEE_STRATEGIES = {\n  credit_card: 3.5,\n  debit_card: 2.0,\n  pix: 0.0 // Adiciona sem modificar lógica\n};\n\nfunction calculateFee(type) {\n  return FEE_STRATEGIES[type] || 0;\n}\n```\n\n#### **2.3. Liskov Substitution Principle (LSP)**\n\nVerificar se classes derivadas podem substituir base sem quebrar.\n\n#### **2.4. Interface Segregation Principle (ISP)**\n\nVerificar se interfaces/DTOs não forçam implementação de métodos desnecessários.\n\n#### **2.5. Dependency Inversion Principle (DIP)**\n\nJá coberto em Clean Architecture (1.1).\n\n---\n\n### **3. Acoplamento**\n\n#### **3.1. Acoplamento Temporal**\n\n```javascript\n// ❌ ERRADO: Ordem de chamadas importa\nawait service.createReceita(data);\nawait service.updateMonthlySummary(); // Precisa ser chamado depois!\n\n// ✅ CORRETO: Service atualiza sumário automaticamente\nawait service.createReceita(data);\n// updateMonthlySummary() é chamado internamente ou via trigger no BD\n```\n\n#### **3.2. Acoplamento de Dados**\n\n```javascript\n// ❌ ERRADO: Componente conhece estrutura interna do banco\nfunction Component() {\n  const data = await fetch('/api/revenues');\n  return <div>{data.revenues[0].gross_amount}</div>; // Campo do banco\n}\n\n// ✅ CORRETO: Componente usa DTO com campos abstraídos\nfunction Component() {\n  const { data } = useReceitas();\n  return <div>{data[0].value}</div>; // Campo do DTO\n}\n```\n\n---\n\n### **4. Código Duplicado**\n\n**Comando de busca:**\n```bash\n# Procurar funções/blocos duplicados com jscpd\nnpx jscpd src/ --min-lines 10 --min-tokens 50\n\n# Procurar imports duplicados\ngrep -rh \"^import\" src/ | sort | uniq -c | sort -rn | head -20\n```\n\n**Padrões comuns de duplicação:**\n\n```javascript\n// ❌ DUPLICADO em múltiplos services\nif (!data.value || data.value <= 0) {\n  return { error: 'Valor deve ser maior que zero' };\n}\n\n// ✅ CORRETO: Extrair para validator/DTO\nclass BaseDTO {\n  validatePositiveNumber(value, fieldName) {\n    if (!value || value <= 0) {\n      throw new Error(`${fieldName} deve ser maior que zero`);\n    }\n  }\n}\n```\n\n---\n\n### **5. Inconsistências de Nomenclatura**\n\n**Verificar:**\n\n| Tipo | Padrão Correto | Exemplo |\n|------|----------------|----------|\n| Arquivos | kebab-case | `financeiro-service.js` |\n| Classes | PascalCase | `FinanceiroService` |\n| Funções | camelCase | `createReceita()` |\n| Constantes | UPPER_SNAKE_CASE | `ALLOWED_REVENUE_COLUMNS` |\n| Componentes React | PascalCase | `ReceitasPage` |\n| Hooks | camelCase com 'use' | `useReceitas` |\n\n**Comando de busca:**\n```bash\n# Procurar arquivos com nomes inconsistentes\nfind src/ -name \"*[A-Z]*\" # PascalCase (incorreto para arquivos)\nfind src/ -name \"*_*\" # snake_case (incorreto para JavaScript)\n```\n\n---\n\n### **6. DTOs Mal Implementados**\n\n#### **6.1. DTO sem Validação**\n\n```javascript\n// ❌ ERRADO: DTO apenas mapeia, não valida\nclass CreateRevenueDTO {\n  constructor(data) {\n    this.value = data.value;\n    this.type = data.type;\n  }\n}\n\n// ✅ CORRETO: DTO valida campos\nclass CreateRevenueDTO {\n  constructor(data) {\n    this.value = data.value;\n    this.type = data.type;\n  }\n  \n  validate() {\n    const errors = [];\n    if (!this.value || this.value <= 0) {\n      errors.push('Valor deve ser maior que zero');\n    }\n    return { isValid: errors.length === 0, errors };\n  }\n}\n```\n\n#### **6.2. DTO sem Whitelist/Blacklist**\n\n```javascript\n// ❌ ERRADO: Aceita qualquer campo (SQL Injection potencial)\ntoDatabase() {\n  return this; // Retorna TUDO, incluindo campos maliciosos\n}\n\n// ✅ CORRETO: Aplica whitelist\ntoDatabase() {\n  const dbObject = {};\n  for (const [key, value] of Object.entries(this)) {\n    if (ALLOWED_COLUMNS.includes(key)) {\n      dbObject[key] = value;\n    }\n  }\n  return dbObject;\n}\n```\n\n**Comando de busca:**\n```bash\n# Procurar DTOs sem método validate()\nfor file in src/dtos/*.js; do\n  if ! grep -q \"validate()\" \"$file\"; then\n    echo \"DTO sem validate(): $file\"\n  fi\ndone\n```\n\n---\n\n### **7. Services Acessando Banco Diretamente**\n\n**Comando de busca:**\n```bash\n# Procurar Services com queries diretas\ngrep -r \"\\.from(\\|.select(\\|.insert(\\|.update(\\|.delete(\" src/services/ --exclude=\"supabase.js\"\n```\n\n**Se encontrar:** Refatorar para usar Repository.\n\n---\n\n### **8. Componentes React Violando Camadas**\n\n```javascript\n// ❌ ERRADO: Page chamando Service diretamente\nimport financeiroService from './services/financeiroService';\n\nfunction ReceitasPage() {\n  useEffect(() => {\n    financeiroService.getReceitas().then(setData);\n  }, []);\n}\n\n// ✅ CORRETO: Page usa Hook\nimport { useReceitas } from './hooks/useReceitas';\n\nfunction ReceitasPage() {\n  const { data, loading } = useReceitas();\n}\n```\n\n**Comando de busca:**\n```bash\n# Procurar Pages importando Services\ngrep -r \"import.*Service\" src/pages/\n```\n\n---\n\n### **9. Falta de Validação**\n\n**Verificar:**\n\n- [ ] DTOs validam tipos (string, number, date)\n- [ ] DTOs validam ranges (value > 0, length <= 255)\n- [ ] DTOs validam ENUMs\n- [ ] DTOs validam UUIDs\n- [ ] Services rejeitam dados inválidos ANTES de persistir\n\n---\n\n### **10. Segurança**\n\n#### **10.1. RLS no Supabase**\n\n**Verificar:**\n\n```sql\n-- Todas as tabelas devem ter RLS ativo\nSELECT tablename, rowsecurity \nFROM pg_tables \nWHERE schemaname = 'public' \n  AND rowsecurity = FALSE;\n```\n\nSe retornar tabelas, ativar RLS:\n\n```sql\nALTER TABLE {table_name} ENABLE ROW LEVEL SECURITY;\n```\n\n#### **10.2. SQL Injection**\n\n```javascript\n// ❌ ERRADO: Concatenação de strings (SQL Injection)\nconst query = `SELECT * FROM revenues WHERE id = '${userId}'`;\n\n// ✅ CORRETO: Uso de prepared statements (Supabase já protege)\nconst { data } = await supabase\n  .from('revenues')\n  .select('*')\n  .eq('id', userId); // Supabase sanitiza automaticamente\n```\n\n#### **10.3. XSS (Cross-Site Scripting)**\n\n```jsx\n// ❌ ERRADO: Renderizar HTML sem sanitizar\nfunction Component({ userInput }) {\n  return <div dangerouslySetInnerHTML={{ __html: userInput }} />;\n}\n\n// ✅ CORRETO: React escapa automaticamente\nfunction Component({ userInput }) {\n  return <div>{userInput}</div>;\n}\n```\n\n---\n\n## 📊 Relatório de Auditoria\n\n### **Template de Relatório**\n\n```markdown\n# 📋 RELATÓRIO DE AUDITORIA DE ARQUITETURA\n\n**Data:** {data_atual}\n**Versão do Sistema:** {versao}\n**Auditor:** Claude AI\n\n---\n\n## 🎯 Resumo Executivo\n\n| Categoria | Violações Encontradas | Severidade |\n|-----------|----------------------|------------|\n| Clean Architecture | {numero} | {Alta/Média/Baixa} |\n| SOLID | {numero} | {Alta/Média/Baixa} |\n| Acoplamento | {numero} | {Alta/Média/Baixa} |\n| Código Duplicado | {numero} | {Alta/Média/Baixa} |\n| Nomenclatura | {numero} | {Alta/Média/Baixa} |\n| DTOs | {numero} | {Alta/Média/Baixa} |\n| Segurança | {numero} | {Alta/Média/Baixa} |\n\n**Total de Violações:** {total}\n**Score de Qualidade:** {score}/100\n\n---\n\n## ❌ Violações Encontradas\n\n### **1. Clean Architecture**\n\n#### Violação #1: Service acessando banco diretamente\n- **Arquivo:** `src/services/financeiroService.js:45`\n- **Severidade:** 🔴 Alta\n- **Descrição:** Service importa `supabase` e faz query direta\n- **Código:**\n  ```javascript\n  const { data } = await supabase.from('revenues').select('*');\n  ```\n- **Solução:** Criar `RevenueRepository` e delegar acesso ao banco\n\n---\n\n### **2. SOLID**\n\n#### Violação #2: SRP - Service com múltiplas responsabilidades\n- **Arquivo:** `src/services/financeiroService.js`\n- **Severidade:** 🟡 Média\n- **Descrição:** Service faz validação + persistência + envio de email\n- **Solução:** Extrair `EmailService` separado\n\n---\n\n## ✅ Recomendações\n\n1. **Criar Repositories faltantes**\n   - ExpenseRepository\n   - ClientRepository\n   - InventoryRepository\n\n2. **Refatorar Services grandes**\n   - Quebrar `financeiroService.js` (500 linhas) em múltiplos services\n\n3. **Adicionar validação em DTOs**\n   - CreateExpenseDTO está sem método `validate()`\n\n4. **Ativar RLS em tabelas**\n   - Tabelas `clients` e `inventory` sem RLS\n\n5. **Remover código duplicado**\n   - Validação de UUID duplicada em 5 arquivos\n   - Extrair para `src/utils/validators.js`\n\n---\n\n## 📈 Métricas de Qualidade\n\n- **Cobertura de Testes:** {percentage}%\n- **Complexidade Ciclomática:** {average}\n- **Linhas de Código:** {total_lines}\n- **Debt Técnico:** {tech_debt_hours} horas\n\n---\n\n*Auditoria realizada em {data_hora}*\n```\n\n---\n\n## 🚀 Comandos de Análise Automática\n\n```bash\n# Instalar ferramentas de análise\nnpm install -D eslint jscpd complexity-report\n\n# Executar análises\nnpx eslint src/ --format json > audit-eslint.json\nnpx jscpd src/ --format json > audit-duplicates.json\nnpx cr src/ --format json > audit-complexity.json\n\n# Gerar relatório consolidado\nnode scripts/generate-audit-report.js\n```\n\n---\n\n## ✅ Checklist Final\n\n- [ ] Auditou Clean Architecture\n- [ ] Auditou SOLID\n- [ ] Auditou acoplamento\n- [ ] Auditou código duplicado\n- [ ] Auditou nomenclatura\n- [ ] Auditou DTOs\n- [ ] Auditou segurança (RLS, SQL Injection, XSS)\n- [ ] Gerou relatório em Markdown\n- [ ] Salvou relatório em `docs/AUDIT_REPORT.md`\n- [ ] Criou issues no GitHub para cada violação\n\n---\n\n**IMPORTANTE:** Priorize violações de **segurança** e **Clean Architecture** antes das demais.\n"
}
